model Setting {
  id        String   @id @default(uuid())
  key       SettingKey
  value     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([key])
  @@map("settings")
}

enum SettingKey {
  TeacherProfit @map("Teacher Profit")
  Currency @map("Currency")
}
